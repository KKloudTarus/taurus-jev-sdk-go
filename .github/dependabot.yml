version: 2

updates:
  # The module has no dependencies outside the standard library, so the only
  # thing to keep current is the actions the workflows pin.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: "ci"
